{
  "name": "generate_data",
  "version": "1.0.0",
  "description": "This is the first \"TP\" of the infra/cloud class",
  "main": "index.js",
  "scripts": {
    "start": "npm run build && node build/index.js",
    "build": "rimraf ./build && tsc",
    "start:dev": "npx nodemon",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Evan Cadet",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^2.0.21",
    "rimraf": "^4.2.0",
    "ts-node": "^10.9.1",
    "typescript": "^4.9.5"
  }
}
